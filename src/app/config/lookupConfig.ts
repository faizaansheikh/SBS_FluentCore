const CreateConfig = (key: string, label: string) => {
    return {
        key,
        label,
    }
}


  export const roleConfig = [
    CreateConfig('Id', 'Id'),
    CreateConfig('RoleName', 'Role Name'),

]